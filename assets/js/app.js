
const BRATSHO = (() => {
  const STORE_KEY = 'bratsho_data_v2';
  const seedListings = [
    {id:'1',title:'هونداي أزيرا 2023',price:168000,city:'طرابلس',category:'cars',year:2023,km:'12000 كم',fuel:'بنزين',condition:'ممتازة',featured:true,tag:'مميز',seller:'براتشو كار',phone:'0910000000',images:[0,1,2,3],desc:'سيارة فخمة، فل أوبشن، مناسبة للباحث عن النظافة والهيبة.',features:['بصمة','كاميرا 360','تبريد مقاعد','بانوراما']},
    {id:'2',title:'جينيسيس G80 2018',price:146000,city:'بنغازي',category:'cars',year:2018,km:'60000 ميل',fuel:'بنزين',condition:'نظيفة',featured:true,tag:'فخم',seller:'يونس',phone:'0911111111',images:[1,2,3,0],desc:'نسخة فخمة مع مواصفات ممتازة.',features:['رادار','ذاكرة','حساسات','مقاعد كهرباء']},
    {id:'3',title:'كيا سورينتو 2019',price:119000,city:'مصراتة',category:'cars',year:2019,km:'74000 كم',fuel:'ديزل',condition:'عائلية',featured:false,tag:'عائلي',seller:'محمد',phone:'0922222222',images:[2,3,0,1],desc:'سبعة مقاعد ومواصفات كاملة.',features:['7 مقاعد','بانوراما','جلد','كاميرا']},
    {id:'4',title:'جنوط رياضية 18',price:2400,city:'طرابلس',category:'accessories',year:2025,km:'جديد',fuel:'-',condition:'جديد',featured:false,tag:'كماليات',seller:'كماليات ليبيا',phone:'0933333333',images:[3,0,1,2],desc:'طقم جنوط بحالة ممتازة.',features:['18','رياضية','لون أسود']},
    {id:'5',title:'علبة دركسيون أزيرا',price:1800,city:'الزاوية',category:'parts',year:2025,km:'استعمال خفيف',fuel:'-',condition:'مستعمل نظيف',featured:false,tag:'قطع غيار',seller:'بيت الغيار',phone:'0944444444',images:[0,3,1,2],desc:'قطعة أصلية ومجربة.',features:['أصلي','مكفول']},
    {id:'6',title:'ميكانيكي متنقل',price:150,city:'طرابلس',category:'services',year:2025,km:'زيارة',fuel:'-',condition:'خدمة',featured:true,tag:'خدمة',seller:'ورشة سريعة',phone:'0955555555',images:[2,1,0,3],desc:'فحص وصيانة في موقعك.',features:['توصيل','كشف أعطال','خدمة سريعة']}
  ];
  const seedChats = [
    {id:'t1',name:'محمد - جينيسيس',last:'السلام عليكم، هل السعر نهائي؟',messages:[{from:'them',text:'السلام عليكم، هل السعر نهائي؟'},{from:'me',text:'وعليكم السلام، قابل للنقاش البسيط.'}]},
    {id:'t2',name:'عميل - أزيرا',last:'ممكن صور أكثر؟',messages:[{from:'them',text:'ممكن صور أكثر؟'},{from:'me',text:'أكيد، هذه نسخة تجريبية ويمكن ربطها بالدردشة الحقيقية.'}]}
  ];
  const seedUser = {name:'زائر براتشو',email:'demo@bratsho.ly',phone:'0910000000',city:'طرابلس',plan:'مجاني',favorites:['1','2'],myListings:['1','4'],isAdmin:true};

  function load() {
    try {
      const raw = localStorage.getItem(STORE_KEY);
      if (raw) return JSON.parse(raw);
    } catch {}
    const data = {listings:seedListings,chats:seedChats,user:seedUser,notifications:[{text:'تم نشر مشروع براتشو كار بنجاح',time:'الآن'}]};
    save(data); return data;
  }
  function save(data){localStorage.setItem(STORE_KEY, JSON.stringify(data));}
  function carSvg(seed=0){const p=[['#0f1720','#1747b9','#9dbdff'],['#111827','#2563eb','#dce7ff'],['#14213d','#3b82f6','#bcd3ff'],['#0b1220','#4f7fff','#dce6ff']][seed%4];return `<svg viewBox="0 0 800 560" xmlns="http://www.w3.org/2000/svg"><defs><linearGradient id="g${seed}" x1="0" x2="1"><stop offset="0" stop-color="#edf4ff"/><stop offset="1" stop-color="#b8ceff"/></linearGradient><linearGradient id="c${seed}" x1="0" x2="1"><stop offset="0" stop-color="${p[0]}"/><stop offset="0.6" stop-color="${p[1]}"/><stop offset="1" stop-color="#0b1220"/></linearGradient></defs><rect width="800" height="560" fill="url(#g${seed})"/><circle cx="680" cy="80" r="40" fill="#fff8"/><path d="M125 355c28-56 74-96 136-118 68-24 165-29 248-12 64 13 132 58 171 122l20 33H95l30-25z" fill="url(#c${seed})"/><path d="M263 245c34-30 101-47 174-45 51 1 93 17 127 47l-36 2c-50 4-141 5-228 2l-37-6z" fill="${p[2]}" opacity=".95"/><circle cx="243" cy="388" r="57" fill="#1c2430"/><circle cx="243" cy="388" r="27" fill="#dfe7f3"/><circle cx="565" cy="388" r="57" fill="#1c2430"/><circle cx="565" cy="388" r="27" fill="#dfe7f3"/><rect x="605" y="338" width="55" height="20" rx="10" fill="#fff2a8" opacity=".95"/></svg>`;}
  function fmtPrice(v){return `${Number(v).toLocaleString('en-US')} د.ل`;}
  function getCategoryName(v){return {cars:'سيارات',parts:'قطع غيار',accessories:'كماليات',services:'خدمات'}[v]||'إعلان';}
  function getId(){return Math.random().toString(36).slice(2,9)}
  function qs(s,root=document){return root.querySelector(s)}
  function qsa(s,root=document){return [...root.querySelectorAll(s)]}
  function toast(msg){alert(msg)}

  function card(item,i=0){return `<article class="card listing"><div class="thumb">${carSvg(i)}<span class="tag">${item.tag||getCategoryName(item.category)}</span><span class="price">${fmtPrice(item.price)}</span></div><div class="body"><h3 class="title">${item.title}</h3><div class="meta"><span>📍 ${item.city}</span><span>📅 ${item.year||'-'}</span><span>🛣️ ${item.km||'-'}</span><span>⛽ ${item.fuel||'-'}</span></div><div class="chips" style="margin-top:10px">${(item.features||[]).slice(0,4).map(x=>`<span class="chip">${x}</span>`).join('')}</div><div style="display:flex;gap:10px;flex-wrap:wrap;margin-top:14px"><a class="btn btn-soft" href="${location.pathname.includes('/pages/') ? 'details.html' : 'pages/details.html'}?id=${item.id}">التفاصيل</a><button class="btn btn-light" data-fav="${item.id}">❤ المفضلة</button></div></div></article>`}

  function renderHome(){
    const data=load(); const root=qs('[data-home-listings]'); if(root) root.innerHTML=data.listings.slice(0,8).map(card).join('');
    const fav=qs('[data-fav-count]'); if(fav) fav.textContent=data.user.favorites.length;
    const my=qs('[data-my-count]'); if(my) my.textContent=data.user.myListings.length;
  }
  function renderCatalog(){
    const data=load(); const root=qs('[data-catalog]'); if(!root) return;
    const params=new URLSearchParams(location.search);
    const q=(qs('[name=q]')?.value || params.get('q') || '').trim().toLowerCase();
    const city=qs('[name=city]')?.value || params.get('city') || '';
    const category=qs('[name=category]')?.value || params.get('category') || '';
    const min=Number(qs('[name=min]')?.value || params.get('min') || 0);
    const max=Number(qs('[name=max]')?.value || params.get('max') || 999999999);
    const filtered=data.listings.filter(x=>(!q || x.title.toLowerCase().includes(q)||x.desc.toLowerCase().includes(q)) && (!city || x.city===city) && (!category || x.category===category) && x.price>=min && x.price<=max);
    const count=qs('[data-results-count]'); if(count) count.textContent=filtered.length;
    root.innerHTML=filtered.length?filtered.map(card).join(''):`<div class="empty">لا توجد نتائج مطابقة الآن.</div>`;
  }
  function renderDetails(){
    const data=load(); const id=new URLSearchParams(location.search).get('id') || '1'; const item=data.listings.find(x=>x.id===id) || data.listings[0];
    const root=qs('[data-details]'); if(!root) return;
    root.innerHTML=`<div class="gallery"><div class="gallery-main card" id="gallery-main">${carSvg(0)}</div><div class="gallery-thumbs">${item.images.map((x,i)=>`<button data-gallery="${i}">${carSvg(i)}</button>`).join('')}</div></div><div class="details-card card"><span class="pill">${item.tag}</span><h1 style="margin:14px 0 8px">${item.title}</h1><div class="meta"><span>📍 ${item.city}</span><span>📅 ${item.year}</span><span>🛣️ ${item.km}</span><span>⛽ ${item.fuel}</span><span>⭐ ${item.condition}</span></div><h2 style="margin:16px 0;color:var(--blue)">${fmtPrice(item.price)}</h2><p class="muted" style="line-height:1.9">${item.desc}</p><div class="chips">${item.features.map(x=>`<span class="chip">${x}</span>`).join('')}</div><div style="display:flex;gap:10px;flex-wrap:wrap;margin-top:18px"><button class="btn btn-primary" data-call="${item.phone}">اتصال</button><button class="btn btn-light" data-whatsapp="${item.phone}">واتساب</button><button class="btn btn-soft" data-fav="${item.id}">إضافة للمفضلة</button></div></div><div class="seller-card card"><h3>بيانات المعلن</h3><p><strong>${item.seller}</strong></p><p class="muted">${item.city}</p><p class="muted">الهاتف: ${item.phone}</p><div class="notice">يمكن لاحقًا ربط هذه الصفحة برسائل مباشرة وتقييمات المستخدمين والتحقق من الحساب.</div></div>`;
    qsa('[data-gallery]').forEach((b,i)=>b.onclick=()=>qs('#gallery-main').innerHTML=carSvg(i));
  }
  function bindFavorites(){
    document.addEventListener('click',e=>{
      const fav=e.target.closest('[data-fav]'); if(!fav) return; const data=load(); const id=fav.dataset.fav; const idx=data.user.favorites.indexOf(id); if(idx>-1){data.user.favorites.splice(idx,1); toast('تمت إزالة الإعلان من المفضلة');} else {data.user.favorites.push(id); toast('تمت إضافة الإعلان إلى المفضلة');} save(data);
    })
  }
  function bindCalls(){document.addEventListener('click',e=>{const c=e.target.closest('[data-call]'); if(c) location.href=`tel:${c.dataset.call}`; const w=e.target.closest('[data-whatsapp]'); if(w) location.href=`https://wa.me/218${w.dataset.whatsapp.replace(/^0/,'')}`;})}
  function bindSearch(){qs('[data-search-submit]')?.addEventListener('click',()=>{const q=qs('[name=hero_q]')?.value||''; const city=qs('[name=hero_city]')?.value||''; const category=qs('[name=hero_category]')?.value||''; const target=location.pathname.includes('/pages/')?'listings.html':'pages/listings.html'; location.href=`${target}?q=${encodeURIComponent(q)}&city=${encodeURIComponent(city)}&category=${encodeURIComponent(category)}`;}); qs('[data-apply-filters]')?.addEventListener('click',()=>renderCatalog())}
  function bindAddListing(){const form=qs('[data-add-form]'); if(!form) return; const thumbs=qs('[data-upload-preview]'); const fileInput=qs('[name=images]'); fileInput?.addEventListener('change',()=>{const count=Math.min(fileInput.files.length,20); thumbs.innerHTML=Array.from({length:count},(_,i)=>`<div class="preview">${i+1}</div>`).join('')}); form.addEventListener('submit',e=>{e.preventDefault(); const fd=new FormData(form); const data=load(); const item={id:getId(),title:fd.get('title'),price:Number(fd.get('price')||0),city:fd.get('city'),category:fd.get('category'),year:Number(fd.get('year')||new Date().getFullYear()),km:fd.get('km'),fuel:fd.get('fuel'),condition:fd.get('condition'),featured:fd.get('featured')==='on',tag:'جديد',seller:data.user.name,phone:data.user.phone,images:[0,1,2,3],desc:fd.get('desc'),features:(fd.get('features')||'').split('،').map(x=>x.trim()).filter(Boolean)}; data.listings.unshift(item); data.user.myListings.unshift(item.id); save(data); toast('تم حفظ الإعلان محليًا في النسخة التجريبية. في النسخة الحقيقية يربط مع Firebase/Backend.'); form.reset(); thumbs.innerHTML='';})}
  function renderAccount(){const root=qs('[data-account]'); if(!root) return; const data=load(); const favs=data.listings.filter(x=>data.user.favorites.includes(x.id)); const mine=data.listings.filter(x=>data.user.myListings.includes(x.id)); root.innerHTML=`<div class="dashboard"><aside class="card side-menu"><a class="active" href="#">لوحة التحكم</a><a href="#my">إعلاناتي</a><a href="#fav">المفضلة</a><a href="admin.html">لوحة الإدارة</a><a href="#">الإعدادات</a></aside><section class="grid"><div class="stats"><div class="card kpi"><strong>${mine.length}</strong><span class="muted">إعلاناتي</span></div><div class="card kpi"><strong>${favs.length}</strong><span class="muted">المفضلة</span></div><div class="card kpi"><strong>${data.chats.length}</strong><span class="muted">المحادثات</span></div><div class="card kpi"><strong>${data.user.plan}</strong><span class="muted">الباقة الحالية</span></div></div><div class="card" style="padding:18px" id="my"><div class="section-head"><h2>إعلاناتي</h2><a class="btn btn-primary" href="add-listing.html">+ إعلان جديد</a></div>${mine.length?`<div class="cards">${mine.map(card).join('')}</div>`:'<div class="empty">لا توجد إعلانات خاصة بك الآن.</div>'}</div><div class="card" style="padding:18px" id="fav"><div class="section-head"><h2>المفضلة</h2></div>${favs.length?`<div class="cards">${favs.map(card).join('')}</div>`:'<div class="empty">لا توجد عناصر في المفضلة.</div>'}</div></section></div>`}
  function renderChat(){const root=qs('[data-chat]'); if(!root) return; const data=load(); const current=data.chats[0]; root.innerHTML=`<div class="chat-layout"><aside class="card chat-list">${data.chats.map((t,i)=>`<div class="thread ${i===0?'active':''}" data-thread="${t.id}"><strong>${t.name}</strong><div class="muted">${t.last}</div></div>`).join('')}</aside><section class="card chat-box"><h3>${current.name}</h3><div data-chat-messages>${current.messages.map(m=>`<div class="bubble ${m.from==='me'?'me':'them'}">${m.text}</div>`).join('')}</div><div class="message-input"><input type="text" placeholder="اكتب رسالتك هنا" data-chat-input><button class="btn btn-primary" data-send-chat>إرسال</button></div></section></div>`; qs('[data-send-chat]')?.addEventListener('click',()=>{const inp=qs('[data-chat-input]'); if(!inp.value.trim()) return; const box=qs('[data-chat-messages]'); box.innerHTML+=`<div class="bubble me">${inp.value}</div>`; inp.value='';})}
  function renderAdmin(){const root=qs('[data-admin]'); if(!root) return; const data=load(); root.innerHTML=`<div class="dashboard"><aside class="card side-menu"><a class="active" href="#">الإحصائيات</a><a href="#ads">الإعلانات</a><a href="#users">المستخدمون</a><a href="#plans">الاشتراكات</a></aside><section class="grid"><div class="stats"><div class="card kpi"><strong>${data.listings.length}</strong><span class="muted">إجمالي الإعلانات</span></div><div class="card kpi"><strong>${data.chats.length}</strong><span class="muted">المحادثات</span></div><div class="card kpi"><strong>12</strong><span class="muted">معلنين موثقين</span></div><div class="card kpi"><strong>3</strong><span class="muted">خطط اشتراك</span></div></div><div class="card" style="padding:18px" id="ads"><div class="section-head"><h2>إدارة الإعلانات</h2><button class="btn btn-light" data-demo-admin>تحديث الحالة</button></div><table class="table"><thead><tr><th>العنوان</th><th>القسم</th><th>السعر</th><th>الحالة</th></tr></thead><tbody>${data.listings.map((x,i)=>`<tr><td>${x.title}</td><td>${getCategoryName(x.category)}</td><td>${fmtPrice(x.price)}</td><td><span class="status ${i%3===0?'ok':i%3===1?'warn':'off'}">${i%3===0?'منشور':i%3===1?'بانتظار': 'مرفوض'}</span></td></tr>`).join('')}</tbody></table></div><div class="cards" id="plans"><div class="card" style="padding:18px"><h3>الخطة المجانية</h3><p class="muted">إعلانان وصور محدودة</p><h2>0 د.ل</h2></div><div class="card" style="padding:18px"><h3>الخطة الشهرية</h3><p class="muted">إعلانات أكثر + ترويج</p><h2>49 د.ل</h2></div><div class="card" style="padding:18px"><h3>الخطة السنوية</h3><p class="muted">أفضل قيمة وإعلانات مميزة</p><h2>399 د.ل</h2></div><div class="card" style="padding:18px"><h3>تثبيت أعلى الصفحة</h3><p class="muted">إعلان ممول</p><h2>20 د.ل</h2></div></div></section></div>`; qs('[data-demo-admin]')?.addEventListener('click',()=>toast('في النسخة الحقيقية يتم ربط الإدارة مع قاعدة البيانات والصلاحيات والمراجعة.'))}
  function renderFavorites(){const root=qs('[data-favorites]'); if(!root) return; const data=load(); const favs=data.listings.filter(x=>data.user.favorites.includes(x.id)); root.innerHTML=favs.length?`<div class="cards">${favs.map(card).join('')}</div>`:'<div class="empty">ما عندكش عناصر مفضلة حتى الآن.</div>'}
  function renderMyListings(){const root=qs('[data-my-listings]'); if(!root) return; const data=load(); const mine=data.listings.filter(x=>data.user.myListings.includes(x.id)); root.innerHTML=mine.length?`<div class="cards">${mine.map(card).join('')}</div>`:'<div class="empty">لا توجد إعلانات منشورة باسمك.</div>'}
  function bindYear(){qsa('[data-year]').forEach(x=>x.textContent=new Date().getFullYear())}
  function bindDemoButtons(){document.addEventListener('click',e=>{const b=e.target.closest('[data-demo]'); if(b) toast('هذه نسخة متقدمة قابلة للتطوير. للنسخة الحقيقية اربط Firebase أو Backend واستضافة إنتاجية.');})}
  function init(){bindYear(); bindFavorites(); bindCalls(); bindSearch(); bindAddListing(); bindDemoButtons(); renderHome(); renderCatalog(); renderDetails(); renderAccount(); renderChat(); renderAdmin(); renderFavorites(); renderMyListings();}
  return {init,load,save};
})();
document.addEventListener('DOMContentLoaded', BRATSHO.init);
